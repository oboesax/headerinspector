PORT=3050
CORS_ORIGINS=http://localhost:3050,https://headers.oboesax.com
# Comma-separated list of headers to strip from output (overrides default proxy header list)
# STRIP_HEADERS=x-forwarded-for,x-forwarded-proto,x-real-ip
